<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

## Project Workflow

- Do not treat local-only edits or local-only data changes as the final resolution for this project.
- When a user asks for a fix, feature, or configuration change, make it as a project-wide change and push it to GitHub unless the user explicitly asks for a local-only experiment.
- Local commands, dev server checks, and browser verification are allowed for validation, but the deliverable should be committed and pushed.
